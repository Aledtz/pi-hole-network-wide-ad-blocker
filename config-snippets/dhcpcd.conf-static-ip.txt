# Static IP configuration for wlan0 (Raspberry Pi Zero W Wi-Fi)
interface wlan0
static ip_address=192.168.1.224/24
static routers=192.168.1.1
static domain_name_servers=127.0.0.1 1.1.1.1

# Prevents the Pi from changing its IP — critical for reliable DNS service